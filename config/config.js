const sessionSecret = "blogSessionUserSecret";

// SMTP Configuraton for forgeting password
const emailUser = "";
const emailPassword = "";

module.exports = {
    sessionSecret,
    emailPassword,
    emailUser
}